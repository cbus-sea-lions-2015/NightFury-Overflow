<% if @question %>
  <%= form_for :comment, url: question_comments_path(@question) do |f| %>
    <fieldset>
      <%= f.label :body, "Add Comment" %>
      <%= f.text_area :body, autofocus: true, value: comment.body %>
    </fieldset>
    <%= f.submit "Submit", :disable_with => 'Submiting...' %>
  <% end %>
<% end %>